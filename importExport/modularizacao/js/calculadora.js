const calculadora = {
    somar: (a, b) => a + b,
    subtrair: (a, b) => a - b
}

export default calculadora


// OUTRA FORMA DE EXPORTAÇÃO
// export default = {
//     somar: (a, b) => a + b,
//     subtrair: (a, b) => a - b
// }